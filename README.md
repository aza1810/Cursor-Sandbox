# Cursor-Sandbox

Sandbox for Cursor Slack / cloud-agent experiments.

## Welcome Lyndan

A simple welcome landing page for **Lyndan**.

Open `index.html` in a browser, or serve the folder:

```bash
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.
